import { CalenderTextPipe } from './calender-text.pipe';

describe('CalenderTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CalenderTextPipe();
    expect(pipe).toBeTruthy();
  });
});
